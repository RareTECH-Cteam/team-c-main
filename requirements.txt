# ================================================================
# Keigo API 依存パッケージ (Django + DRF)
# バージョンは 2026-07 時点の安定版を固定
# ================================================================

# --- Django + DRF ---
# Django 5.2 LTS
Django==5.2.4
# DRF: REST API
djangorestframework==3.15.2
# COR制御: Cross-Origin リクエスト許可
django-cors-headers==4.5.0
# API検索・絞り込みを楽にする
django-filter==24.3
# API ドキュメント自動生成
drf-spectacular==0.28.0

# --- WSGI / ASGI サーバー ---
# gunicorn
gunicorn==23.0.0
# uvicorn
uvicorn[standard]==0.32.0

# --- DB ドライバ ---
# pPostgreSQLへ接続するためのドライバ
psycopg[binary]==3.3.4

# --- 設定管理 ---
# django-environ: settings.py で環境変数を型付きで読み込む
django-environ==0.11.2

# --- 認証 (JWTを扱うPythonライブラリ) ---
PyJWT[crypto]==2.10.0
cryptography==44.0.0

# --- AWS SDK ---
boto3==1.35.60
botocore==1.35.60

# --- AI (Gemini)用SDK ---
google-genai==0.3.0

# --- 決済 (Stripe) ---
stripe==11.3.0

# --- HTTP クライアント ---
# 外部 API 呼び出し用 (非同期対応)
httpx==0.27.2

# --- ロギング ---
# アプリログをJSON形式で出力
structlog==24.4.0

# --- 開発用 ---
# ipythonで起動、Pythonの対話実行環境を強化
ipython==8.29.0
# Pythonコードの品質チェックツール
ruff==0.7.4
